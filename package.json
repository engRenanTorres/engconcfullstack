{
  "devDependencies": {
    "turbo": "^1.10.3"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "node apps/backend/dist/main",
    "format": "turbo run format"
  },
  "workspaces": [
    "apps/*"
  ]
}
